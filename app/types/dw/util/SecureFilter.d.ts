
/**
 * SecureFilter contains many methods for manipulating untrusted data Strings into
 * RFC-Compliant Strings for a given context by removing "bad" data from the
 * untrusted data.
 *
 * This class does not have a constructor, so you cannot create it directly.
 */
declare class SecureFilter {
    private constructor();


}

export = SecureFilter;
